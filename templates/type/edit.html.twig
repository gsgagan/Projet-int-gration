{% extends 'base.html.twig' %}

{% block title %}Modifier un Type{% endblock %}

{% block body %}
<div class="container mt-5">
    <h2 class="mb-4">Modifier un Type</h2>

    <div class="card shadow p-4">
        {{ include('type/_form.html.twig', {'button_label': 'Modifier'}) }}
    </div>

    <div class="mt-3">
        <a href="{{ path('app_type_index') }}" class="btn btn-outline-primary">Retour à l'index</a>
    </div>
</div>
{% endblock %}
